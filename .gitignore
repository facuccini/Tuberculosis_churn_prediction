# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# ── Environments ─────────────────────────────────────────
venv/
.venv/
env/
.env
*.env

# ── Jupyter ──────────────────────────────────────────────
.ipynb_checkpoints/

# ── Data (synthetic — regenerated by data/generate_tb_data.py) ──
data/raw/
data/processed/
# raw/ is a stale artifact directory at project root — safe to ignore
raw/

# ── Models (binary pickles — large, environment-specific) ─
models/*.pkl

# ── Reports (generated outputs) ──────────────────────────
reports/top10_*.csv

# NOTE: figures/ is intentionally NOT ignored — PNGs are
# committed so GitHub renders them inline in the README.

# ── OS / IDE ─────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ── Stale artifact directories (created by bad shell cmd) ─
{data
